import apiClient from './apiClient';
import { API_ENDPOINTS } from '../constants/apiEndpoints';

/**
 * User, Auth & Address Service Module
 * References API_ENDPOINTS.AUTH_REGISTER, AUTH_LOGIN, AUTH_ME, ADDRESS_GEOCODE, ADDRESS_REVERSE_GEOCODE, ADDRESSES, ADDRESS_DETAILS, SET_DEFAULT_ADDRESS
 * TODO: Endpoints will be implemented once integration begins.
 */
export const userService = {};

export default userService;

